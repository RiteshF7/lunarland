package com.termux.app

/**
 * Simple Kotlin class used to verify that Kotlin sources compile correctly.
 */
class SampleKotlin {
    fun greet(name: String = "Termux"): String = "Hello, $name from Kotlin!"
}

